// we use loader as it is more optimized than use effect as use effect fetches when
// we click that button but loader starts fetching when we are about to click it starts
// fetching on that hover thing only and store it in cache
 